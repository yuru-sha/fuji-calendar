{
  "name": "@fuji-calendar/types",
  "version": "0.4.0",
  "description": "Shared type definitions for Fuji Calendar",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/ && find dist -name '*.ts' -exec sh -c 'mv \"$1\" \"${1%.ts}.d.ts\"' _ {} \\; && echo 'module.exports = {};' > dist/index.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "files": [
    "dist"
  ]
}